{
  "name": "dsh-fabric",
  "version": "0.1.11",
  "description": "Fabric-style deterministic compaction, activity, topology, code runtime, mesh, and actor capabilities for DeepSeek Harness",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "packageManager": "bun@1.4.0",
  "files": [
    "cordis.patch.yml",
    "README.md",
    "LICENSE",
    "THIRD_PARTY_NOTICES.md",
    "ADAPTATION_SWEEP.md"
  ],
  "exports": {
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "dependencies": {
    "@monotykamary/dsh-agent-presets": "^0.1.7",
    "dsh-fabric-client-ui": "workspace:^",
    "dsh-fabric-code-runtime-quickjs": "workspace:^",
    "dsh-fabric-compaction": "workspace:^",
    "dsh-fabric-host": "workspace:^",
    "dsh-fabric-mesh": "workspace:^",
    "dsh-fabric-models": "workspace:^",
    "dsh-fabric-protocol": "workspace:^",
    "dsh-fabric-schema": "workspace:^",
    "dsh-fabric-system-prompt": "workspace:^"
  },
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "bun run clean && bun run --filter dsh-fabric-protocol build && bun run --filter dsh-fabric-compaction build && bun run --filter dsh-fabric-system-prompt build && bun run --filter dsh-fabric-host build && bun run --filter dsh-fabric-mesh build && bun run --filter dsh-fabric-models build && bun run --filter dsh-fabric-schema build && bun run --filter dsh-fabric-code-runtime-quickjs build && bun run --filter dsh-fabric-client-ui build",
    "watch:client": "bun run --filter dsh-fabric-client-ui watch",
    "install:local": "node scripts/install-local.mjs",
    "uninstall:local": "node scripts/install-local.mjs --uninstall",
    "typecheck": "bun run --filter dsh-fabric-protocol typecheck && bun run --filter dsh-fabric-protocol build && bun run --filter dsh-fabric-compaction typecheck && bun run --filter dsh-fabric-compaction build && bun run --filter dsh-fabric-system-prompt typecheck && bun run --filter dsh-fabric-system-prompt build && bun run --filter dsh-fabric-host typecheck && bun run --filter dsh-fabric-host build && bun run --filter dsh-fabric-mesh typecheck && bun run --filter dsh-fabric-mesh build && bun run --filter dsh-fabric-models typecheck && bun run --filter dsh-fabric-models build && bun run --filter dsh-fabric-schema typecheck && bun run --filter dsh-fabric-code-runtime-quickjs typecheck && bun run --filter dsh-fabric-client-ui typecheck",
    "test": "vitest run",
    "verify": "node scripts/verify-workspace.mjs",
    "check": "bun run typecheck && bun run build && bun run test && bun run verify",
    "link:harness": "node scripts/link-harness.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "lightningcss": "^1.32.0",
    "semver": "^7.8.5",
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/monotykamary/dsh-fabric.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "trustedDependencies": [
    "esbuild"
  ]
}
