# @dsh-fabric/protocol

Host-independent records shared by dsh-fabric adapters, services, and renderers. It defines cycle-safe session lineage, general activity/topology nodes and edges, bounded projection values, revisioned state, durable topics, and crash-conservative actor mailbox records. It imports no DSH host or browser package.
